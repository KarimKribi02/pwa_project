# TODO (auto)
- [ ] Fix “View details” modal in `frontend/src/app/admin/billing/page.tsx` to show products (current code looks for `activeFacture.commandes?.produits` only).
- [ ] Add/ensure vertical scroller inside the “Facture …” popup so it can scroll top/bottom.
- [ ] Verify UI by opening Admin > Billing and clicking the Eye icon.
